modules:
  # steam-devices is in Fedora repos; steam requires RPMFusion non-free
  - type: dnf
    repos:
      nonfree: rpmfusion
    install:
      install-weak-deps: false
      packages:
        - steam-devices
        - steam
