modules:
  - type: dnf
    install:
      packages:
        - steam-devices

  - type: default-flatpaks
    configurations:
      - notify: true
        scope: system
        install:
          - com.valvesoftware.Steam
